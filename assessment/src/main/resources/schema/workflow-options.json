{
    "$id": "classpath:/schema/workflow-options.json",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Workflow Options",
    "description": "Output schema for workflow option output",
    "type": "object",
    "properties": {
        "result": {
            "type" : "array",
            "description": "Workflow name list",
            "items": {"$ref" : "wf-option-schema.json"}
        }
    },
    "required": ["wfOptions"]
}